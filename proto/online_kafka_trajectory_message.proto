syntax = "proto3";

package tracking_pipeline.online.kafka;

import "event.proto";
import "online_kafka.proto";

message TrajectoryMessage {
    UUID uuid = 1;
    MessageHeader message_header = 2;

    Trajectory trajectory = 8; // Trajectory is from event.proto
}
