/* Proto definition for vtinfos */

syntax = "proto3";

package mall.vtinfosmap;

message VTInfosMap {
  map<string, VTInfo> vt_infos_map = 1;
}

message VTInfo {
  // default value "0" means not-found/no-match from staff database.
  repeated float reid_feats = 4;
}
