syntax = "proto3";

package tracking_pipeline.online.kafka;

import "online_kafka.proto";

message PersonEventVisitCounterMessage {
    UUID uuid = 1;
    MessageHeader message_header = 2;

    PersonEventVisitCounter event = 8;
}
